# Local Claude Code configuration (machine-specific paths, not for publishing).
.claude/

# Output of a local `jekyll serve`; GitHub Pages builds the published site.
_site/
.jekyll-cache/
.jekyll-metadata
